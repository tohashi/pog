<div class="center">
  <h1>pog</h1>
  <p>プレイ中・積み中のゲームを記録しておく事ができます。</p>
  <% if !@user.guest? %>
    <a href="#!" class="button button-big button-blue">pile</a>
  <% end %>
</div>

<% if false %>
  <%= link_to 'Delete account', delete_account_path(@user.id), method: :delete %>
<% end %>


<div class="nav mobile-nav-table nav-tabs">
  <a href="#!" class="active">24h</a>
  <a href="#!">1week</a>
  <a href="#!">Total</a>
</div>

<h5>60min</h5>
<ul>
  <% @ranking_60min.each do |ranking| %>
    <% content = ranking[:content] %>
    <li>
      <%= link_to content.name, content_path(content.id) %>
      <%= ranking[:count] %>
    </li>
  <% end %>
</ul>

<h5>all</h5>
<ul>
  <% @ranking_all.each do |ranking| %>
    <% content = ranking[:content] %>
    <li>
      <%= link_to content.name, content_path(content.id) %>
      <%= ranking[:count] %>
    </li>
  <% end %>
</ul>
